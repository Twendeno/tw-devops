tls:

  certificates:
    - certFile: "/etc/ssl/traefik/cert.pem"
      keyFile: "/etc/ssl/traefik/privkey.pem"
      stores:
        - default

  stores:
    default:
      defaultCertificate:
        certFile: "/etc/ssl/traefik/cert.pem"
        keyFile: "/etc/ssl/traefik/privkey.pem"